#include "utest.h"
#include "../src/Pixel.h"
#include "../src/TranslatedPixel.h"

UTEST(TranslatedPixel, basic) {
    // TranslatedPixel tp;

    // rgb_t colour;
    // colour.red = 20;
    // Pixel p(10, 10, colour);

    // std::vector<Pixel> & pixels;
    // pixels.push_back(p);

    // {

    //     tp = TranslatedPixel(pixels, 1);
    // }

    // ASSERT_EQ(tp.getX(), 10);

    ASSERT_TRUE(1);
}

UTEST(TranslatedPixel, basic2) {
    std::vector<Pixel> pixels;

    ASSERT_TRUE(1);
}